{
  "name": "Apply-SAML-Credentials-To-AWS-Config",
  "version": "0.1",
  "description": "Gets AWS credentials from samlresponse and saves them to a local config file.",
  "main": "lib",
  "types": "lib",
  "author": "Austin Mayer",
  "license": "MIT",
  "scripts": {
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^18.7.13"
  },
  "dependencies": {
    "dotenv": "^16.0.1"
  }
}
